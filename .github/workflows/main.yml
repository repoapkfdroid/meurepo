name: Gerar Catalogo F-Droid Oficial

on:
  push:
    branches: [ "main", "principal" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install fdroidserver
        run: |
          sudo apt-get update
          sudo apt-get install -y fdroidserver android-sdk-build-tools

      - name: Build F-Droid Repository
        run: |
          # 1. Garante que a pasta repo existe
          mkdir -p repo
          
          # 2. Gera a keystore se não existir
          keytool -genkey -v -keystore keystore.p12 -storetype PKCS12 -alias repo -keyalg RSA -keysize 2048 -validity 10000 -storepass 123456 -keypass 123456 -dname "CN=FDroidRepo"

          # 3. Cria a configuração base do F-Droid
          cat << 'EOF' > config.yml
          repo_url: "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/repo"
          repo_name: "Meu Repositorio F-Droid"
          repo_icon: "icon.png"
          repo_description: "Repositorio de Aplicativos"
          archive_older: 0
          EOF

          # 4. Processa os APKs e cria os indices
          fdroid update --create-metadata --keystore keystore.p12 --repo-keyalias repo --keystorepass 123456 || fdroid update --keystore keystore.p12 --repo-keyalias repo --keystorepass 123456

          # 5. Prepara os arquivos para o GitHub Pages
          mkdir -p public
          cp -r repo public/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'public'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
